#!/bin/bash
# v1 MVP 測試腳本 - 支援自動判斷和人工檢視
#
# 使用方式：
#   ./run_v1_tests.sh          # 人工判斷模式（預設）
#   ./run_v1_tests.sh --auto   # 自動判斷模式
#   ./run_v1_tests.sh --help   # 顯示說明

# 顏色定義
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
NC='\033[0m' # No Color

# 全域變數
AUTO_MODE=false
TOTAL_TESTS=0
PASSED_TESTS=0
FAILED_TESTS=0
SKIPPED_TESTS=0

# 解析參數
if [[ "$1" == "--auto" ]]; then
    AUTO_MODE=true
elif [[ "$1" == "--help" || "$1" == "-h" ]]; then
    echo "v1 MVP 測試腳本"
    echo ""
    echo "使用方式："
    echo "  ./run_v1_tests.sh          人工判斷模式（預設）"
    echo "  ./run_v1_tests.sh --auto   自動判斷模式"
    echo "  ./run_v1_tests.sh --help   顯示此說明"
    echo ""
    echo "自動判斷模式："
    echo "  - 自動比對實際結果與預期結果"
    echo "  - 顯示詳細的差異資訊"
    echo "  - 統計通過/失敗測試數量"
    echo "  - 判斷項目：品項、金額、付款方式、分類、意圖"
    echo "  - 不判斷：交易ID（每次都不同）"
    echo ""
    exit 0
fi

echo "======================================================================"
echo "🧪 v1 MVP 測試腳本"
echo "======================================================================"
echo ""
if [[ "$AUTO_MODE" == true ]]; then
    echo "模式：🤖 自動判斷"
else
    echo "模式：👤 人工判斷"
    echo ""
    echo "使用方式："
    echo "  - 每個測試案例會顯示測試訊息和結果"
    echo "  - 按 Enter 繼續下一個測試"
    echo "  - 按 Ctrl+C 中斷測試"
fi
echo ""
read -p "按 Enter 開始測試..."

# 提取欄位值的輔助函數
extract_field() {
    local output="$1"
    local field="$2"

    # 根據不同的欄位格式提取
    case "$field" in
        "intent")
            echo "$output" | grep "📝 意圖:" | sed 's/.*📝 意圖: //' | xargs
            ;;
        "品項")
            echo "$output" | grep "🛍️  品項:" | sed 's/.*🛍️  品項: //' | xargs
            ;;
        "金額")
            echo "$output" | grep "💰 金額:" | sed 's/.*💰 金額: TWD //' | xargs
            ;;
        "付款")
            echo "$output" | grep "💳 付款:" | sed 's/.*💳 付款: //' | xargs
            ;;
        "分類")
            echo "$output" | grep "🏷️  分類:" | sed 's/.*🏷️  分類: //' | xargs
            ;;
        "日期")
            echo "$output" | grep "📅 日期:" | sed 's/.*📅 日期: //' | xargs
            ;;
        "交易ID")
            echo "$output" | grep "🆔 交易ID:" | sed 's/.*🆔 交易ID: //' | xargs
            ;;
        "response")
            echo "$output" | grep "💬 回應:" | sed 's/.*💬 回應: //' | xargs
            ;;
    esac
}

# 比較結果
compare_result() {
    local actual="$1"
    local expected="$2"
    local field_name="$3"

    # 如果預期值為空，跳過檢查
    if [[ -z "$expected" ]]; then
        return 0
    fi

    # 金額比較（允許浮點數）
    if [[ "$field_name" == "金額" ]]; then
        # 移除可能的 .0
        actual=$(echo "$actual" | sed 's/\.0$//')
        expected=$(echo "$expected" | sed 's/\.0$//')
    fi

    # 分類比較（允許部分匹配）
    if [[ "$field_name" == "分類" ]]; then
        if [[ "$actual" == *"$expected"* ]] || [[ "$expected" == *"$actual"* ]]; then
            return 0
        fi
    fi

    # 一般字串比較
    if [[ "$actual" == "$expected" ]]; then
        return 0
    else
        return 1
    fi
}

# 測試函數（人工判斷模式）
run_test_manual() {
    local category="$1"
    local description="$2"
    local message="$3"

    echo ""
    echo "======================================================================"
    echo "[$category] $description"
    echo "======================================================================"
    echo ""

    python test_local.py --v1 "$message"

    echo ""
    read -p "按 Enter 繼續下一個測試..."
}

# 測試函數（自動判斷模式）
run_test_auto() {
    local category="$1"
    local description="$2"
    local message="$3"
    local expected_intent="$4"
    local expected_item="$5"
    local expected_amount="$6"
    local expected_payment="$7"
    local expected_category="$8"

    ((TOTAL_TESTS++))

    echo ""
    echo -e "${BLUE}======================================================================"
    echo "[$category] $description"
    echo -e "======================================================================${NC}"
    echo "📨 測試訊息: $message"

    # 執行測試並捕獲輸出
    output=$(python test_local.py --v1 "$message" 2>&1)

    # 提取實際結果
    actual_intent=$(extract_field "$output" "intent")
    actual_item=$(extract_field "$output" "品項")
    actual_amount=$(extract_field "$output" "金額")
    actual_payment=$(extract_field "$output" "付款")
    actual_category=$(extract_field "$output" "分類")
    actual_transaction_id=$(extract_field "$output" "交易ID")

    # 判斷測試結果
    test_passed=true
    failures=()

    # 檢查意圖
    if ! compare_result "$actual_intent" "$expected_intent" "意圖"; then
        test_passed=false
        failures+=("意圖: 預期[$expected_intent] 實際[$actual_intent]")
    fi

    # 檢查品項（只在記帳意圖時）
    if [[ "$expected_intent" == "記帳" ]] && [[ -n "$expected_item" ]]; then
        if ! compare_result "$actual_item" "$expected_item" "品項"; then
            test_passed=false
            failures+=("品項: 預期[$expected_item] 實際[$actual_item]")
        fi
    fi

    # 檢查金額
    if [[ -n "$expected_amount" ]]; then
        if ! compare_result "$actual_amount" "$expected_amount" "金額"; then
            test_passed=false
            failures+=("金額: 預期[$expected_amount] 實際[$actual_amount]")
        fi
    fi

    # 檢查付款方式
    if [[ -n "$expected_payment" ]]; then
        if ! compare_result "$actual_payment" "$expected_payment" "付款"; then
            test_passed=false
            failures+=("付款: 預期[$expected_payment] 實際[$actual_payment]")
        fi
    fi

    # 檢查分類
    if [[ -n "$expected_category" ]]; then
        if ! compare_result "$actual_category" "$expected_category" "分類"; then
            test_passed=false
            failures+=("分類: 預期包含[$expected_category] 實際[$actual_category]")
        fi
    fi

    # 顯示結果
    if [[ "$test_passed" == true ]]; then
        echo -e "${GREEN}✅ 測試通過${NC}"
        ((PASSED_TESTS++))
    else
        echo -e "${RED}❌ 測試失敗${NC}"
        echo -e "${YELLOW}差異項目：${NC}"
        for failure in "${failures[@]}"; do
            echo -e "  ${RED}✗${NC} $failure"
        done
        ((FAILED_TESTS++))
    fi

    # 顯示實際結果摘要
    if [[ "$expected_intent" == "記帳" ]]; then
        echo ""
        echo "📊 實際結果："
        echo "  品項: $actual_item"
        echo "  金額: $actual_amount"
        echo "  付款: $actual_payment"
        echo "  分類: $actual_category"
        if [[ -n "$actual_transaction_id" ]]; then
            echo "  交易ID: $actual_transaction_id （僅供參考，不比對）"
        fi
    elif [[ "$expected_intent" == "對話" ]]; then
        actual_response=$(extract_field "$output" "response")
        echo ""
        echo "📊 實際結果："
        echo "  回應: $actual_response"
    fi

    echo ""

    # 自動模式下短暫停頓讓使用者看清結果
    sleep 0.3
}

# 選擇測試函數
run_test() {
    if [[ "$AUTO_MODE" == true ]]; then
        run_test_auto "$@"
    else
        run_test_manual "$1" "$2" "$3"
    fi
}

# ============================================================
# 基本記帳功能
# ============================================================

run_test "基本功能" "TC-V1-001: 標準記帳格式" \
    "午餐120元現金" \
    "記帳" "午餐" "120" "現金" "餐飲"

run_test "基本功能" "TC-V1-002: 順序不固定" \
    "200 點心 狗卡" \
    "記帳" "點心" "200" "台新狗卡" "點心"

run_test "基本功能" "TC-V1-003: 含小數金額" \
    "咖啡45.5元 Line轉帳" \
    "記帳" "咖啡" "45.5" "Line 轉帳" "飲品"

# ============================================================
# 日期與時間處理
# ============================================================

run_test "日期處理" "TC-V1-010: 語義化日期 - 今天" \
    "今天午餐120元現金" \
    "記帳" "午餐" "120" "現金" "餐飲"

run_test "日期處理" "TC-V1-011: 語義化日期 - 昨天" \
    "昨天晚餐200元狗卡" \
    "記帳" "晚餐" "200" "台新狗卡" "餐飲"

run_test "日期處理" "TC-V1-012: 數字日期格式 (11/12)" \
    "11/12 午餐120元現金" \
    "記帳" "午餐" "120" "現金" "餐飲"

run_test "日期處理" "TC-V1-013: 完整日期格式" \
    "2025-11-10 咖啡50元現金" \
    "記帳" "咖啡" "50" "現金" "飲品"

run_test "日期處理" "TC-V1-014: 無日期（預設今天）" \
    "午餐120元現金" \
    "記帳" "午餐" "120" "現金" "餐飲"

# ============================================================
# 付款方式識別
# ============================================================

run_test "付款方式" "TC-V1-020: 現金" \
    "午餐120元現金" \
    "記帳" "午餐" "120" "現金" "餐飲"

run_test "付款方式" "TC-V1-021: Line 轉帳（暱稱）" \
    "午餐120元Line" \
    "記帳" "午餐" "120" "Line 轉帳" "餐飲"

run_test "付款方式" "TC-V1-022: 台新狗卡（暱稱）" \
    "午餐120元狗卡" \
    "記帳" "午餐" "120" "台新狗卡" "餐飲"

run_test "付款方式" "TC-V1-023: FlyGo 信用卡（暱稱）" \
    "午餐120元灰狗" \
    "記帳" "午餐" "120" "FlyGo 信用卡" "餐飲"

run_test "付款方式" "TC-V1-024: 合庫轉帳" \
    "午餐120元合庫" \
    "記帳" "午餐" "120" "合庫轉帳" "餐飲"

run_test "付款方式" "TC-V1-025: 台新 Richart" \
    "午餐120元Richart" \
    "記帳" "午餐" "120" "台新 Richart" "餐飲"

# ============================================================
# 品項與分類
# ============================================================

run_test "品項分類" "TC-V1-030: 早餐分類" \
    "早餐50元現金" \
    "記帳" "早餐" "50" "現金" "早餐"

run_test "品項分類" "TC-V1-031: 午餐分類" \
    "午餐120元現金" \
    "記帳" "午餐" "120" "現金" "午餐"

run_test "品項分類" "TC-V1-032: 晚餐分類" \
    "晚餐200元現金" \
    "記帳" "晚餐" "200" "現金" "晚餐"

run_test "品項分類" "TC-V1-033: 點心分類（預設家庭）" \
    "花磚甜點200元現金" \
    "記帳" "花磚甜點" "200" "現金" "點心"

run_test "品項分類" "TC-V1-034: 含商家名稱（無介詞）" \
    "義美蛋糕120元現金" \
    "記帳" "義美蛋糕" "120" "現金" "點心"

run_test "品項分類" "TC-V1-035: 含商家名稱（有介詞）" \
    "在開飯吃午餐120元現金" \
    "記帳" "午餐" "120" "現金" "餐飲"

run_test "品項分類" "TC-V1-036: 含用途說明" \
    "饅頭做早餐使用200元現金" \
    "記帳" "饅頭" "200" "現金" ""

# ============================================================
# 自然語句處理
# ============================================================

run_test "自然語句" "TC-V1-040: 自然語句 - 買了" \
    "買了咖啡50元，Line轉帳" \
    "記帳" "咖啡" "50" "Line 轉帳" "飲品"

run_test "自然語句" "TC-V1-041: 自然語句 - 花了" \
    "晚餐吃麵，花了80，用狗卡" \
    "記帳" "" "80" "台新狗卡" ""

run_test "自然語句" "TC-V1-042: 自然語句 - 複雜描述" \
    "今天買了星巴克咖啡，花了150元，用Line轉帳付款" \
    "記帳" "咖啡" "150" "Line 轉帳" "飲品"

# ============================================================
# 對話意圖識別
# ============================================================

run_test "對話意圖" "TC-V1-050: 打招呼" \
    "你好" \
    "對話" "" "" "" ""

run_test "對話意圖" "TC-V1-051: 詢問功能" \
    "可以幫我做什麼？" \
    "對話" "" "" "" ""

run_test "對話意圖" "TC-V1-052: 感謝" \
    "謝謝" \
    "對話" "" "" "" ""

# ============================================================
# 錯誤處理
# ============================================================

run_test "錯誤處理" "TC-V1-060: 缺少金額" \
    "午餐現金" \
    "對話" "" "" "" ""

run_test "錯誤處理" "TC-V1-061: 缺少付款方式" \
    "午餐120元" \
    "對話" "" "" "" ""

run_test "錯誤處理" "TC-V1-062: 缺少品項" \
    "120元現金" \
    "對話" "" "" "" ""

# ============================================================
# 完成
# ============================================================

echo ""
echo "======================================================================"
echo "✅ v1 MVP 測試完成！"
echo "======================================================================"
echo ""

if [[ "$AUTO_MODE" == true ]]; then
    # 自動模式：顯示統計資訊
    echo "📊 測試統計："
    echo "  - 總測試數: $TOTAL_TESTS"
    echo -e "  - ${GREEN}通過: $PASSED_TESTS${NC}"
    echo -e "  - ${RED}失敗: $FAILED_TESTS${NC}"

    if [[ $SKIPPED_TESTS -gt 0 ]]; then
        echo -e "  - ${YELLOW}跳過: $SKIPPED_TESTS${NC}"
    fi

    echo ""

    # 計算通過率
    if [[ $TOTAL_TESTS -gt 0 ]]; then
        pass_rate=$((PASSED_TESTS * 100 / TOTAL_TESTS))
        echo "📈 通過率: $pass_rate%"
        echo ""

        if [[ $pass_rate -eq 100 ]]; then
            echo -e "${GREEN}🎉 所有測試通過！${NC}"
        elif [[ $pass_rate -ge 80 ]]; then
            echo -e "${YELLOW}⚠️  大部分測試通過，但仍有失敗項目需要修復${NC}"
        else
            echo -e "${RED}❌ 測試失敗率過高，需要檢查問題${NC}"
        fi
    fi
else
    # 人工模式：顯示基本資訊
    echo "測試統計："
    echo "  - 基本功能：3 個測試"
    echo "  - 日期處理：5 個測試"
    echo "  - 付款方式：6 個測試"
    echo "  - 品項分類：7 個測試"
    echo "  - 自然語句：3 個測試"
    echo "  - 對話意圖：3 個測試"
    echo "  - 錯誤處理：3 個測試"
    echo "  - 總計：30 個測試"
fi

echo ""
